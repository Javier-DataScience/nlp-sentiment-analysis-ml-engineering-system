# ============================================================
# PYTEST CONFIGURATION
# ------------------------------------------------------------
# PURPOSE:
# Defines default behavior for test execution in ML project.
# Ensures clean, fast, and deterministic test runs.
# ============================================================

[pytest]

testpaths = tests
python_files = test_*.py
addopts = -v --disable-warnings